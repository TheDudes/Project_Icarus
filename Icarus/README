-README from Icarus interpreter-

to build/run/... apache-ant is needed.

you have to be inside the Icarus folder, where the build.xml is located.
do not forget to configure ur OWN! (which is NOT under version control) config
file, and specify it inside the Main.java file (/Icarus/src/Icarus/Main.java).

Usage:
    ant [compile|javadoc|run|all|magic]

compile -> will compile
javadoc -> creates javadoc
run     -> run
all     -> compile, javadoc, create jar and run
magic   -> compile, create jar and run
